@model IEnumerable<Pagina_Ventas.Models.dbModels.Pago>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.MetodosPagoPagos)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DescripcionPagos)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MetodosPagoPagos)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DescripcionPagos)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdPagos">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdPagos">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdPagos">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
